表关系结构：
1、采区包含多个煤层，一个煤层有多个工作面，物理所属关系中，工作面又包含多条巷道，巷道中包含多个测点；同时测点属于抽采单元，抽采单元属于工作面。
2、远端负压值：测点属性中有是否为管路最远端标识，做特殊查询用到；测点属性中的measuring_point_level_id字段对应的为该测点是统计工作面总抽采
    量或者巷道抽采量：如统计工作面的总抽采量的标识为1，统计传感器数据中所有该属性为1的数据，做计算即为该工作面的抽采量
3、数据库中传感器的数据为所有计量装置的数据统计，具体类的传感器数据需分类处理。一个测点对应多个类型的传感器。

抽采动态统计：
1、实时数据中有实时纯量，混合量，此信息都保存到数据库中。
2、计算单日的累计纯量只需要得到当天最后一包数据解析出来即可；计算日累计混量，需要
将当天最后一包数据减去当天的第一包数据，得到混合量。浓度为纯量除以混量即可得到。
3、计算多日抽采量（S--E）：
    A、不跨月的情况下，获取E的最后一包数据，然后获取S的第一包数据，E月抽采纯量-S月抽采纯量为这几天的抽采纯量，混量同理。
    B、跨月情况：先获取开始日期S所在月的最后一天的最后一包数据，减去开始日期，得到x；获取E的最后一包数据得到y；x+y即为所得。
3、线程控制，每天凌晨计算各个测点的累积量保存到redis中，方便读取。
5、通过实时数据得到测点基本信息，保存到数据库中后加上字段index,来确定测点的先后顺序。测点之间是串联关系，测点关联抽采单元，
要计算某抽采单元的量，即通过关联的测点X及上方测点Y的量，用Y-X即为该抽采单元的量。


2023.02.11  替换人员定位查询：{{baseurl}}/personneinfo/getPersonneinfo ----------> {{baseurl}}/personneinfo/list


2023.05.23  线程PerceptionDataThread，实时监测CDDY信息、FZDY信息，有数据加载到内存中，判断数据库中跟内存中的是否一致：一致不动；不一致更新。提供接口可获取CDDY在数据库中的数据
2023.05.31  更改类sensorType

2023.06.28  设计钻孔中添加设计参数字段，用于关联设计参数字段。添加设计参数的同时，插入设计钻孔数据
            确定方案：提供四种设计参数的集合，将符合这些设计参数的所有设计钻孔标识（sign）改成1   -----废弃


2023.06.29  1、设计钻孔中添加sign(标识：default 0)，确定方案的同时，将设计参数和包含的钻孔的标识改成1。
            2、通过工作面ID可以分别查询所有的设计参数和已确定方案的设计参数。
            3、定向钻点位信息表关联到钻孔数据表中。添加钻孔信息的同时，增加点位信息。

2023.06.30  1、保存设计方案（方案名称、工作面ID、设计参数ID列表），将方案相关信息保存到数据库中，方案ID添加到设计参数中的scheme_id(为多个方案ID，以,分割)
            2、确定方案，通过方案ID更改方案标识，设计参数标识，设计参数标识

2023.07.10  1、导出Excel实钻模版，八矿现场台帐数据和实钻应用数据两个，实现下拉格式的数据。

2023.07.12  1、测点基本信息表中添加抽采单元ID和位置顺序字段；查询某一个抽采单元的抽采数据是上级数据减去本级数据。

2023.07.13  1、在解析抽采实时数据的线程中添加方法：以测点编码作为抽采量的唯一属性（作为key）,实时更新测点的累计抽采量
			2、analysisCDSS();方法中，每次读取CDSS数据更新Redis中的累计数据
2023.07.14  1、通过抽采单元获取抽采数据。（测点基本信息表中，关联抽采单元，同时设置测点安装位置的序号，要获取某一个抽采单元的
			抽采数据要看该抽采单元的序号，序号为最大的，则该抽采测点的数据即为该抽采单元的数据）
			
2023.07.19  1、四个设计参数合并，提取公共参数，其他信息保存到detailed_content，其他逻辑不变。

2023.07.21  1、创建表t_sensordata_before，用于保存没有接入系统前的抽采量

2023.07.24  1、接口getDataByDrainageUnitId根据抽采单元ID获取抽采数据中添加没有接入系统时，已经抽采的量
设置测点基础信息和抽采单元的关系：
	1、首先获取到所有的测点安装位置 group by measure_point_local
	2、根据现场实际情况，设置测点位置是否为管路（总）测点，将piping改为1（后面统计抽采单元只统计piping为1的测点）
	3、设置测点安装位置属于某一个巷道（update roadway_id = ? where measure_point_local in (……)）
	4、设置某一测点安装位置的抽采单元和
2023.07.31 新建表t_drainagedata_record，记录抽采动态中的统计类数据，每天凌晨设置定时器，统计昨天的日累计量，若当天为月初一号，则同时统计上个月的月累计量保存到数据库中。

2023.08.09 获取所有抽采单元的抽采纯量数据：系统安装前的抽采纯累计量（t_drainagedata_befor）+抽采数据累计表中的累计抽采纯量（t_drainagedata_record）+当天的实时纯量为该
			测点的抽采量，减去排序下方的测点纯量值，即为抽采数据

抽采动态统计原理：
	1、抽采数据中间表（t_drainagedata_record），系统正常运行中，每天的凌晨某个时间点，查询昨天的抽采历史数据表（t_bk_sensor_data）中的抽采累计数据的最后包数据，
	查看中间表中是否有该条数据，没有的话进行插入操作，保证中间表中的数据为每天的抽采累计数据。
	2、1中将数据保存到中间表中以后，查询出来中间表中的数据，看当天是否为月初的第一天，若为月初的第一天，则将昨天的月累计数据取出，增加到抽采数据累计表（t_drainagedata_sum）
	累加到字段gas_scalar上面，更新字段gas_scalar_month；若不为月初的第一天，则只更新字段gas_scalar_month；
	3、系统安装前的抽采数据，抽采累计纯量需要保存到表（t_drainagedata_before）中。
	4、计算抽采单元的抽采量：遍历出所有的抽采单元，通过抽采单元的ID。
	计算抽采累计纯量：获取到该抽采单元对应的总管路的测点编码，通过测点编码获取到当前时间的抽采历史数据中的月累计抽采纯量X，通过抽采单元ID，获取到该抽采单元在抽采累计
	表（t_drainagedata_sum）中的累计抽采纯量Y，通过测点编码，获取到t_drainagedata_before中的对应的系统安装前的抽采累计量Z，X+Y+Z及为该抽采单元总管路上的总抽采累计纯量值。
	计算瓦斯抽放量：通过测点编码，查询抽采历史数据中当前时间的抽放量，即为所需值。
	可能存在的情况：
	1、系统没有启动，几天之后，系统中间表中没有数据：需手动访问接口填充数据到中间表中：接口为 /tBKSensorData/handMovementInsertData?startDate=2023-06-25&endDate=2023-07-02

2025年9月9日，更新xml，根据数据库不同使用的获取当前时间的函数不同，兼容mysql和sqlite：需修改yml文件，database-id、driver-class-name、url。